{% extends 'base.html.twig' %}
{% block main_class %}signup-form wrapper{% endblock %}
{% block content %}
    <div class="tour-title">Регистрация на {{ tour.tourIndex }} тур "{{ tour.olymp.name}}"</div>
    {{ form_start(form) }}
    {{ form_widget(form) }}
        <button class="btn" type="submit">{{ button_label|default('Записаться') }}</button>
    {{ form_end(form) }}

{% endblock %}
{% block styles %}
    <style>
        .tour-title{
            margin:10px 0;
            font-weight: 600;
        }
    </style>
{% endblock %}


