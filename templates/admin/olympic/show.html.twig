{% extends 'admin/base.html.twig' %}

{% block title %}olympic{% endblock %}

{% block body %}
    <h1>olympic</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ olympic.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ olympic.name }}</td>
        </tr>
        <tr>
            <th>Type</th>
            <td>{{ olympic.type }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_olympic_index') }}">back to list</a>

    <a href="{{ path('admin_olympic_edit', {'id': olympic.id}) }}">edit</a>

    {{ include('admin/olympic/_delete_form.html.twig') }}
{% endblock %}
