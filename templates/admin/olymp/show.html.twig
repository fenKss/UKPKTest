{% extends 'admin/base.html.twig' %}

{% block title %}Olymp{% endblock %}

{% block body %}
    <h1>Olymp</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ olymp.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ olymp.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ olymp.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_olymp_index') }}">back to list</a>

    <a href="{{ path('admin_olymp_edit', {'id': olymp.id}) }}">edit</a>

    {{ include('admin/olymp/_delete_form.html.twig') }}
{% endblock %}
